# وثائق مشروع بصيرة

## نظرة عامة
تطبيق تعليمي صوتي للمكفوفين كلياً من عمر 14 سنة فما فوق. يدعم المراحل التعليمية من التاسع حتى التعليم الجامعي.

## البنية الأساسية

### الترتيب الهرمي للمحتوى
1. **Grade (الصف الدراسي)** - مثل: صف تاسع، البكالوريا، التعليم الجامعي
2. **Category (المادة الدراسية)** - مثل: الرياضيات، اللغة العربية - مرتبطة بصف
3. **Lesson (الدرس)** - مرتبط بمادة دراسية

## النماذج الجديدة

### 1. Quiz (الاختبارات)
- أنواع الاختبارات: تمرين، امتحان، اختبار قصير
- مرتبطة بمادة دراسية ودرس (اختياري)
- إعدادات: المدة، نقاط النجاح، الحد الأقصى للمحاولات

### 2. Question (الأسئلة)
- أنواع الأسئلة:
  - اختيار من متعدد
  - صحيح/خطأ
  - سؤال مفتوح
  - إجابة صوتية
- مرتبطة باختبار

### 3. QuestionChoice (خيارات السؤال)
- للأسئلة متعددة الخيارات
- تحدد الإجابة الصحيحة

### 4. QuizAttempt (محاولة الاختبار)
- تتبع محاولات الطلاب
- حساب النتيجة تلقائياً
- تتبع الوقت المستغرق

### 5. Answer (الإجابات)
- إجابات الطلاب على الأسئلة
- التصحيح التلقائي للموضوعية
- يحتاج مراجعة يدوية للمفتوحة

### 6. VolunteerRole (دور المتطوع)
- أدوار المتطوعين:
  - مراجع محتوى
  - مفروغ صوتي
  - مراجع اختبارات
  - منشئ محتوى
  - مدير

## الميزات الجديدة

### 1. دعم PDF والنص
- `Lesson.pdf_file`: ملف PDF الأصلي
- `Lesson.text_content`: محتوى نصي مباشر
- `TextConversionService`: خدمة لاستخراج النص من PDF

### 2. تتبع وقت الاستماع
- `LessonProgress.listening_time_seconds`: الوقت المستغرق في الاستماع

### 3. نظام الاختبارات الصوتية
- اختبارات صوتية كاملة
- تصحيح تلقائي للموضوعية
- مراجعة يدوية للمفتوحة من المتطوعين

### 4. نظام المتطوعين
- إدارة أدوار المتطوعين
- تتبع المهام المكتملة
- نظام مراجعة المحتوى

## API Endpoints

### الدروس
- `GET /api/lessons/` - قائمة الدروس
- `GET /api/lessons/{id}/` - تفاصيل الدرس
- `POST /api/lessons/{id}/update_progress/` - تحديث التقدم
- `GET /api/lessons/{id}/get_progress/` - الحصول على التقدم

### الاختبارات
- `GET /api/quizzes/` - قائمة الاختبارات
- `GET /api/quizzes/{id}/` - تفاصيل الاختبار
- `POST /api/quizzes/{id}/start_attempt/` - بدء محاولة جديدة
- `GET /api/quizzes/{id}/my_attempts/` - محاولات المستخدم

### محاولات الاختبار
- `GET /api/quiz-attempts/` - قائمة المحاولات
- `POST /api/quiz-attempts/{id}/submit_answer/` - إرسال إجابة
- `POST /api/quiz-attempts/{id}/submit_attempt/` - إنهاء المحاولة

### المتطوعين
- `GET /api/volunteer-roles/` - قائمة الأدوار
- `GET /api/volunteer-roles/my_roles/` - أدوار المستخدم

## الخطوات التالية

1. إنشاء migration للنماذج الجديدة:
   ```bash
   python manage.py makemigrations
   python manage.py migrate
   ```

2. تحديث الواجهة الأمامية (Flutter) لدعم:
   - عرض الاختبارات
   - حل الاختبارات صوتياً
   - عرض أدوار المتطوعين
   - دعم PDF في عرض الدروس

3. إضافة Signal لتحويل PDF تلقائياً عند الرفع

4. تحديث Admin Panel للتجربة
